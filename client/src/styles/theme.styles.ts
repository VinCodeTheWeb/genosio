import { ITheme } from './theme.interface';

export const THEME: ITheme = {
    typography: {
        fontFamily: `'Josefin Sans', sans-serif`,
        fontWeight: 400,
        lineHeight: 1.6,
    },
};
