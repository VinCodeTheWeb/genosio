import { IHeader } from './header.type';

export const styles = (): IHeader => ({
    header: {
        backgroundColor: 'white',
        boxShadow: 'none',
    },
    logo: {
        height: '140px',
    },
});
